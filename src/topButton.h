#ifndef TOPBUTTON_H
#define TOPBUTTON_H

// Draw the go-to-top button (call from main drawing loop)
void DrawGoUpButton(void);

// Unload the goUp texture (call during cleanup)
void UnloadGoUpTexture(void);

#endif // TOPBUTTON_H